# Gap fixture: the hand-edit trap — llm.provider switched without clearing
# squilla_router.tier_profile used to hard-fail validation.
[llm]
provider = "zhipu"
model = "dummy-model"

[squilla_router]
enabled = true
tier_profile = "openrouter"

[squilla_router.tiers.c1]
provider = "zhipu"
model = "dummy-tier-model"
