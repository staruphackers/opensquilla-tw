# Gap fixture: a channel type that is no longer registered (configurable only
# in the earliest release) used to reject the whole file even when disabled.
[llm]
provider = "openrouter"
model = "dummy/model"

[[channels.channels]]
type = "msteams"
name = "legacy-teams"
enabled = false

[[channels.channels]]
type = "telegram"
name = "kept-telegram"
enabled = false
token = "dummy-telegram-token"
